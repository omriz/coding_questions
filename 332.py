def print_ints(M,N):
    pass
    # We know a/b can't be larger than M/2 because we'll be doubling back
    # a+b = M
    # a X b = N
    # (M - b) X b = N
    # M X b - b X b = N
    # M X b= N
    b = M X N
